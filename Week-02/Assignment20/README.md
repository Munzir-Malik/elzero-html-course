# Audio Element

### Task Description

Add an audio element to the page with multiple file formats.

### Requirements

* Add an audio element that contains at least three audio extensions.

* Display a message if the browser does not support the audio file.

* The audio must:

    Play automatically when the page loads

    Restart automatically after it ends (loop)