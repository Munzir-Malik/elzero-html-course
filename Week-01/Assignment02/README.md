# First Page

### Task 

1. Create a website contains `[html, head, body]` elements.

2. Th page name is `My First Page`.

3. Make your site SEO friendly by writing `This Is Description For My First Page` on description.

4. Add the element that can contains `CSS` code inside it.

5. Add the element that can contains `JS` code inside it.

6. Add the element that can call external `CSS` code.

7. Write `This Is My First Page` on your site.