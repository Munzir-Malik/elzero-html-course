# HTML Form Assignment

### Tasks
- Create a form that sends data to the same page using GET method
- Add a hidden token field
- Add an email field with a predefined value and make it read-only
- Add a username field with validation and autofocus on page load
- Add a checkbox list for programming skills allowing multiple selections
- Ensure each checkbox label activates its input when clicPked
- Add a radio button list for job titles allowing only one selection
- Ensure one option is checked by default in each group
- Group related inputs using fieldset and legend
- Add a select menu with optgroup options
- Add a textarea for user input
- Add submit and reset buttons